/**
 * leaflet-routing-machine ships without TypeScript types.
 * We only rely on the side effect of extending `L` after import; routing is typed locally in components.
 */
declare module "leaflet-routing-machine";
